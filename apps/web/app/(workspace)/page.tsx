export default function WorkspacePage() {
  // The workspace layout handles everything — this page is a no-op shell
  // required by Next.js App Router to render the (workspace)/layout.tsx
  return null;
}
